import React from "react";
import "../Placeholders.css";

/**
 * TabBarPlaceholder component.
 * Temporary placeholder for the browser's tab strip.
 * Replaced in Batch 08 (Tab Management).
 */
export function TabBarPlaceholder(): React.JSX.Element {
  return (
    <div className="tabbar-placeholder">
      <span>TabBar — Batch 08</span>
    </div>
  );
}

export default TabBarPlaceholder;
